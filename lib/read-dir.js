// chokibare original: directory listing that replaces readdirp (Node streams) for the engine.
// STUB — loads so sibling modules can be required; W12 replaces the body.
'use strict'

function readDir() {
  throw new Error('chokibare: lib/read-dir.js is a stub until W12 lands')
}

module.exports = { readDir }
